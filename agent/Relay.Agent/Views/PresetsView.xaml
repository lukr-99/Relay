<UserControl x:Class="Relay.Agent.Views.PresetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24" MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="Presets" Style="{StaticResource H1}" Margin="0,0,0,6"/>
            <TextBlock Text="Named, switchable whole decks. The selected one is active — edited in the Deck editor and pushed to your phone."
                       Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,0,0,18"/>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <ListBox x:Name="PresetList" Height="300" SelectionChanged="Preset_Selected"
                             Background="Transparent" BorderThickness="0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                        <Button Content="New" Click="New_Click"/>
                        <Button Content="Duplicate" Margin="8,0,0,0" Click="Duplicate_Click"/>
                        <Button Content="Rename" Margin="8,0,0,0" Click="Rename_Click"/>
                        <Button Content="Delete" Style="{StaticResource DangerButton}" Margin="8,0,0,0" Click="Delete_Click"/>
                    </StackPanel>
                    <TextBlock x:Name="Hint" Style="{StaticResource Muted}" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
